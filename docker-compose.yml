services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: post
      POSTGRES_PASSWORD: post
      POSTGRES_DB: post_db
    ports:
      - "5433:5432"
    volumes:
      - post_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U post -d post_db"]
      interval: 3s
      timeout: 3s
      retries: 10

  api:
    build: .
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+asyncpg://post:post@postgres:5432/post_db
      AUTH_BASE_URL: http://host.docker.internal:8001
      AWS_REGION: eu-south-1
      AWS_ENDPOINT_URL: http://host.docker.internal:4566
      S3_PUBLIC_ENDPOINT_URL: http://localhost:4566
      AWS_ACCESS_KEY_ID: test
      AWS_SECRET_ACCESS_KEY: test
      SNS_TOPIC_ARN: arn:aws:sns:eu-south-1:000000000000:post-events
      S3_BUCKET: post-media
      LOG_LEVEL: INFO
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8002:8002"

volumes:
  post_pg:
